name: Broken Link Checker
on:
  schedule:
    # * is a special character in YAML so you have to quote this string
    - cron: "0 13 * * *"
jobs:
  broken-link-checker:
    runs-on: ubuntu-latest
    steps:
      - name: blc
        run: npx -p broken-link-checker blc http://adrianjost.dev/projects
