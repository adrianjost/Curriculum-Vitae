{
	"name": "functions",
	"description": "Cloud Functions for Firebase",
	"license": "MIT",
	"scripts": {
		"lint": "eslint . --fix",
		"serve": "npx -p firebase-tools firebase serve --only functions",
		"shell": "npx -p firebase-tools firebase functions:shell",
		"start": "npm run shell",
		"deploy": "npx -p firebase-tools firebase deploy --only functions",
		"logs": "npx -p firebase-tools firebase functions:log"
	},
	"dependencies": {
		"camelcase": "^6.2.1",
		"firebase-admin": "~10.0.0",
		"firebase-functions": "^3.16.0",
		"glob": "^7.2.0",
		"marked": "^4.0.5"
	},
	"devDependencies": {
		"eslint": "^7.32.0",
		"eslint-plugin-promise": "^5.2.0",
		"firebase-tools": "^9.23.3"
	},
	"engines": {
		"node": "12"
	}
}
